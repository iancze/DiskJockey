name: AKSco

out_base: output/AKSco/

# parameters and starting guess
# format is parameter : [guess, jump]

grid:
  nr: 64
  ntheta: 32 # if mirror about the equator, total of 64
  nphi: 1
  r_in: 0.5 # [AU] # Inner extent of disk
  r_out: 300. # [AU] # Outer extent of disk

# Which channels of the dataset to exclude.
# exclude: [35, 36, 37, 38]

parameters:
  M_star: [2.44, 0.008] # [M_sun] stellar mass
  r_c: [14.2, 0.01] # [AU] characteristic radius
  T_10: [91.2, 0.03] # [K] temperature at 10 AU
  q: [0.51, 0.003] # temperature gradient exponent
  gamma: [1.0, 0.0] # surface density gradient
  logM_CO: [-0.69, 0.01] # [M_earth] disk mass of CO
  ksi: [0.31, 0.003] # [km/s] microturbulence
  dpc: [141., 0.05] # [pc] distance
  incl: [109.32, 0.03] # [degrees] inclination
  PA: [141.09, 0.05] # [degrees] position angle
  vel: [-26.08, 0.001] # [km/s]
  mu_RA: [0.054, 0.002] # [arcsec] centroid location
  mu_DEC: [0.044, 0.002] # [arcsec]

# Name of the covariance matrix, if using. Otherwise comment out. Replaces jumps
# specified under `parameters`
opt_jump: data/AKSco/opt_jump.npy
# Factor by which to scale the jumps (ie covariance matrix is multiplied by
# this *squared*)
jump_scale : 1.0

# where the run%02/jud01, run%02/jud02, temporary subdirectories will be written
base_dir: /scratch/
# base_dir: /pool/cf/iczekala/scratch/
# base_dir : /home/ian/Grad/Research/Disks/JudithExcalibur/testrun/

# The HDF5 file containing the visibilities
data_file: data/AKSco/AKSco_305kHz_2015-2-24.hdf5


# Image setup
npix: 256
pixsize : 38568201867193.242 # [cm], copied from a previous image.out
# Calculate this number by using plot_model.jl to make a test run
# using the same grid settings.

# MCMC setup
samples: 4000
