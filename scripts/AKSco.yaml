name: AKSco

out_base: output/AKSco/

# parameters and starting guess
# format is parameter : [guess, jump]

parameters:
  M_star: [2.40, 0.005] # [M_sun] stellar mass
  r_c: [12.87, 0.05] # [AU] characteristic radius
  T_10: [91.5, 0.1] # [K] temperature at 10 AU
  q: [0.54, 0.001] # temperature gradient exponent
  gamma: [1.0, 0.0] # surface density gradient
  logM_CO: [-0.1, 0.01] # [M_earth] disk mass of CO
  ksi: [0.29, 0.003] # [km/s] microturbulence
  dpc: [141.7, 0.1] # [pc] distance
  incl: [108.7, 0.1] # [degrees] inclination
  PA: [141.2, 0.05] # [degrees] position angle
  vel: [-26.03, 0.002] # [km/s]
  mu_RA: [0.062, 0.002] # [arcsec] centroid location
  mu_DEC: [0.059, 0.002] # [arcsec]

# Name of the covariance matrix, if using. Otherwise comment out. Replaces jumps
# specified under `parameters`
opt_jump: opt_jump_AKSco.npy
# Factor by which to scale the jumps (ie covariance matrix is multiplied by
# this *squared*)
jump_scale : 0.5

# where the run%02/jud01, run%02/jud02, temporary subdirectories will be written
base_dir: /scratch/
# base_dir: testrun/

# The HDF5 file containing the visibilities
data_file: data/AKSco/AKSco.hdf5

# Image setup
npix: 256

# MCMC setup
samples: 4000
